html
    head
        title Success
        meta(name='description', content='Home page')
        meta(charset='utf-8')
        meta(name='viewport', content='width=device-width, initial-scale=1')
        if redirect
            meta(http-equiv='refresh', content = '2; url=' + redirectLink)
        link(rel='stylesheet', href='css/success-failure.css')
    body
        div#vis-holder
            div
                div
                    p#success.display #{message}
            div
                p Redirecting to #{redirectPageName}...